<!-- Add the following three tags inside head. -->
    <meta itemprop="name" content="<%= content_for?(:title) ? yield(:title) : 'Web app development: courses and training @ Coder Factory' %>">
    <meta itemprop="description" content="<%= content_for?(:description) ? yield(:description) : 'See our new improved programmes that help you become a confident coder. Programmes start every month.' %>">
    <meta itemprop="image" content="<%= content_for?(:image) ? yield(:image) : asset_path('class.jpg') %>">
    <!-- Twitter tags -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:site" content="@coderfactory">
    <meta name="twitter:title" content="<%= content_for?(:title) ? yield(:title) : 'Web app development: courses and training @ Coder Factory' %>">
    <meta name="twitter:description" content="<%= content_for?(:description) ? yield(:description) : 'See our new improved programmes that help you become a confident coder. Programmes start every month.' %>">
    <meta name="twitter:creator" content="<%= content_for?(:author) ? yield(:author) : '@coderfactory' %>">
    <meta name="twitter:image:src" content="<%= content_for?(:image) ? yield(:image) : asset_path('logo.png') %>">
    <!-- Open Graph -->
    <meta property="og:title" content="<%= content_for?(:title) ? yield(:title) : 'Web app development: courses and training @ Coder Factory' %>" />
    <meta property="og:image" content="<%= content_for?(:image) ? yield(:image) : asset_path('logo.png') %>" /> 
    <meta property="og:description" content="<%= content_for?(:description) ? yield(:description) : 'See our new improved programmes that help you become a confident coder. Programmes start every month.' %>" /> 